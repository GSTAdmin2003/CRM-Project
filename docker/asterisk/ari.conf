; =============================================================================
; ARI (Asterisk REST Interface) Configuration
; NOTE: Password is set via entrypoint from ARI_PASSWORD env var.
; =============================================================================

[general]
enabled=yes
pretty=yes
allowed_origins=*

[crm]
type=user
read_only=no
password=ARI_PASSWORD_PLACEHOLDER
