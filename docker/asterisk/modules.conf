; =============================================================================
; Asterisk Modules Configuration
; =============================================================================

[modules]
autoload=yes

; Disable modules we don't need to reduce memory footprint
noload => chan_sip.so
noload => chan_skinny.so
noload => chan_mgcp.so
noload => chan_iax2.so
